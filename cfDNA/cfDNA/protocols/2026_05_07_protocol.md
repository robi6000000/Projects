# 2026-05-07

## Server outage — job restart setback

The cluster went down at approximately 14:22 today, killing all running jobs. SLURM requeued them from scratch — no checkpointing, all progress was lost. Connection to the server was unavailable for about an hour around the outage.

## Jobs that completed before the outage

- cv_ly_heavy_train 1717435_13 (fsr, LY train) — COMPLETED May 7 07:25, elapsed 1d 21h
  - Output: data/matrix_internal_ly_train/svm_by_feature/svm_linear_pca150.0_gc_mapq30/cv/svm_linear_pca150.0_gc_mapq30_fsr_cv_probs.csv
  - Note: gc_correction silently disabled for fsr (not in GC_CORRECT_FEATURES) — expected behaviour

## Main setback — Cristiano cv_med pfe and ocf

Jobs were originally submitted May 1, had been running ~2.5 days before being killed. Restarted from scratch at 14:22. Based on other completed cv_med jobs (coverage/ends: ~3d17h, ifs/wps: ~2d21h), expect completion around May 10.

- cv_med 1717425_7 — pfe, restarted May 7 14:22
- cv_med 1717425_10 — ocf, restarted May 7 14:22

## Other restarted jobs

- cv_ly_med_train 1717429_[7-12] — pfe, coverage, ends, ocf, ifs, wps (LY train), restarted May 7 14:22
- cv_rbf0_fsr 1717722_13 — fsr mapq=none RBF CV1, restarted May 7 14:22

## Unaffected jobs

- build_eoedm15 / build_cposedm15 — mapq15 matrix builds, submitted today — COMPLETED
- feat_mapq45 1718490 — Cristiano mapq45 feature computation, array 1-459%20 — COMPLETED
- cv_rbf fsr mapq15/30/45 (1717723/1724/1725) — still in queue

## Note on LY train fsr runtime

LY train fsr finished in 1d 21h vs Cristiano fsr in 2d 12h despite near-identical sample counts (465 vs 459). Likely explained by class distribution differences between the two cohorts affecting SVM convergence. cv_repeats=10 confirmed in the log — no accidental 1-repeat submission.

## What's next

- Wait for cv_med pfe and ocf to finish (~May 10) to complete Cristiano 10x10 CV
- ~~Once build_eoedm15/cposedm15 finish, run RBF CV1 at mapq15 for those features~~ done
- ~~Once feat_mapq45 finishes, build eoedm/cposedm matrices at mapq45, then run RBF CV1~~ done
- Once cv_ly_med_train finishes, train PreveLynch ensemble

## Cristiano per-feature model training (submitted May 7)

Training all 14 features on Cristiano (linear, PCA=150, GC correction, mapq30, cv_repeats=1).
Models will save to `data/matrix/svm_by_feature/svm_linear_pca150.0_gc_cv1_mapq30/models/`.
Note: submitted with cv_repeats=1 — models land in the cv1 folder, separate from the 10x10 CV folder (svm_linear_pca150.0_gc_mapq30) where pfe/ocf are still running.
**TODO: once training completes, move/rename model files from `svm_linear_pca150.0_gc_cv1_mapq30/models/` into `svm_linear_pca150.0_gc_mapq30/models/` so models and CV probs are co-located.**

- train_light 1719297 — array 0-6 (length edm iedm eedm eoedm cposedm fsd), 8G — SUBMITTED
- train_med — array 7-12 (pfe coverage ends ocf ifs wps), 16G — SUBMITTED
- train_heavy — array 13-13 (fsr), 32G — SUBMITTED

```bash
sbatch --job-name=train_light --mem=8G  --array=0-6   scripts/generalized_modelling/gen_svm.sbatch train data/matrix linear false true 150 10 30
sbatch --job-name=train_med   --mem=16G --array=7-12  scripts/generalized_modelling/gen_svm.sbatch train data/matrix linear true  true 150 10 30
sbatch --job-name=train_heavy --mem=32G --array=13-13 scripts/generalized_modelling/gen_svm.sbatch train data/matrix linear false true 150 10 30
```

Next after training completes + pfe/ocf CV finishes (~May 10): build meta-matrix, run ensemble CV and train.

## fsr CV1 recomputation — run_tag 'fix' (submitted May 7)

Recomputing fsr 1×10 CV to investigate AUC discrepancy (0.743 in cv1 vs 0.840 in 10×10 CV).
Output will go to `data/matrix/svm_by_feature/svm_linear_pca150.0_gc_cv1_mapq30_fix/cv/`.

```bash
sbatch --job-name=cv_heavy_fix --mem=64G --array=13-13 scripts/generalized_modelling/gen_svm.sbatch cv data/matrix linear false true 150 1 30 fix
```

## RBF CV1 — eoedm + cposedm at mapq15 and mapq45 (submitted May 7)

Both mapq15 and mapq45 matrices confirmed present in `data/matrix/by_feature/`. Submitted 4 jobs.

```bash
sbatch --job-name=cv_rbf15_eoedm   --mem=8G --array=4-4 scripts/generalized_modelling/gen_svm.sbatch cv data/matrix rbf false false 150 1 15
sbatch --job-name=cv_rbf15_cposedm --mem=8G --array=5-5 scripts/generalized_modelling/gen_svm.sbatch cv data/matrix rbf false false 150 1 15
sbatch --job-name=cv_rbf45_eoedm   --mem=8G --array=4-4 scripts/generalized_modelling/gen_svm.sbatch cv data/matrix rbf false false 150 1 45
sbatch --job-name=cv_rbf45_cposedm --mem=8G --array=5-5 scripts/generalized_modelling/gen_svm.sbatch cv data/matrix rbf false false 150 1 45
```

After all finish:
```bash
python scripts/modelling/evaluate_models.py data/matrix/svm_by_feature/svm_rbf_cv1_mapq15/cv
python scripts/modelling/evaluate_models.py data/matrix/svm_by_feature/svm_rbf_cv1_mapq45/cv
```
