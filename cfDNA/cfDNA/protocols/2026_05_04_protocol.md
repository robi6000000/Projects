# 2026-05-04

## PreveLynch SVM Training (10x10 CV) - needs a rerun because of server maintenance interrupting the calls
Currently, the main focus was using the train set so that we can evaluate the performance on the test subset. 
```
sbatch --job-name=cv_ly_light_train   --mem=8G --array=0-6  scripts/generalized_modelling/gen_svm.sbatch cv_old data/matrix_internal_ly_train linear true true 150 10 30 
```
```
sbatch --job-name=cv_ly_med_train     --mem=24G --array=7-12 scripts/generalized_modelling/gen_svm.sbatch cv_old data/matrix_internal_ly_train linear true true 150 10 30 
```
```
sbatch --job-name=cv_ly_heavy_train   --mem=64G --array=13-13 scripts/generalized_modelling/gen_svm.sbatch cv_old data/matrix_internal_ly_train linear true true 150 10 30 
```

Thre 10x10 CV of LY train set is so far 1 hr in. At the same time, 10x10 CV with the same config is running on the entire Cristiano dataset. 10x10 CV usually runs for 4 days, the cristiano has been running for 1 day so far.

What is ahead: 
- CRISTIANO:
    - Finish 10x10 CV on the entire dataset - currently 10/14 features are done, the 4 remaining are pfe, coverage, ends, ocf - server maintenance is 8am to 9am 2026_05_05, so could need a rerun if not finished by then. the current running job ids are: cv_med - 1717160_[7-10]
    - if 10x10 CV doesnt finish in time, the commands to rerun are: 
    ```
    sbatch --job-name=cv_med     --mem=24G --array=7-10 scripts/generalized_modelling/gen_svm.sbatch cv_old data/matrix linear true true 150 10 30 
    ```
    - Use 10x10 CV result probabilities as meta-matrix for an ensemble model - train ensemble - likely to run fast
    - evaluate trained ensemble on external data - genoscan(entire), prevelynch(20% test set)
    - *possibly* train and save a per-feature model for each of 14 features
- PREVELYNCH:
    - Finish 10x10 CV on the train set - needs a complete rerun because of maintenance, the aborted job ids are cv_ly_med_train - 1717234_[7-12], cv_ly_heavy_train - 1717234_13
    - Use 10x10 CV result probabilities as meta-matrix for an ensemble model - train ensemble - save 
    - evaluate trained ensemble on test set (20% held out)
    - evaluate trained ensemble on external data - genoscan(entire)
    - *possibly* train and save a per-feature model for each of 14 features
- GENOSCAN:
    - just use for external testing
- OVERALL:
    - write a markdown of pipeline workflow / steps
    - implement ensemble
    - check if individual training of per feature models is correct


