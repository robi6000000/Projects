# 2026-05-11

## Memory pressure investigation

cv_ly_med_train (1717429, 7-12) running 3d7h at 24G, peak RSS ~21.8G — within normal range for Cristiano med features (~21.5G). Left running.

cv_crc_med (1719745, 7-12) running at 24G with similar pressure concern. Cancelled and resubmitted at 32G:
```bash
scancel 1719745
sbatch --job-name=cv_crc_med --mem=32G --array=7-12 scripts/generalized_modelling/gen_svm.sbatch cv data/matrix_crc linear true true 150 10 30
```
New job: 1719864.

Note: test inference jobs (gen_svm_test) only use ~3G even for med features — 16G is sufficient with large headroom.

## Cristiano ensemble — completed

All 14 Cristiano 10×10 CV probs confirmed present. Built meta-matrix and submitted ensemble CV + train:

```bash
conda activate cfdna
python scripts/generalized_processing/meta_matrix_build.py \
    data/matrix/svm_by_feature/svm_linear_pca150.0_gc_mapq30/cv/ \
    data/matrix/svm_by_feature/svm_linear_pca150.0_gc_mapq30/meta_matrix/ \
    data/manifest/Cristiano_metadata.csv

sbatch --job-name=ensemble_cv    scripts/generalized_modelling/gen_ensemble_svm.sbatch cv    data/matrix/svm_by_feature/svm_linear_pca150.0_gc_mapq30/meta_matrix/meta_matrix.csv linear 10 none none none none
sbatch --job-name=ensemble_train scripts/generalized_modelling/gen_ensemble_svm.sbatch train data/matrix/svm_by_feature/svm_linear_pca150.0_gc_mapq30/meta_matrix/meta_matrix.csv linear 10 none none none none
```

Both completed. Ensemble 10×10 CV AUC: **0.9932**.

Bug fix: gen_ensemble_svm.py called `cv_svm_old()` which no longer exists in CFDNAModel. Fixed to `cv_svm()`.

Output:
- CV probs: `data/matrix/svm_by_feature/svm_linear_pca150.0_gc_mapq30/ensemble/cv/cv_probs.csv`
- Model: `data/matrix/svm_by_feature/svm_linear_pca150.0_gc_mapq30/ensemble/models/ensemble.pkl`

## CRC training completed + test jobs submitted

CRC per-feature training (0-12, light+med) completed. Submitted test jobs on LY and GS:

```bash
sbatch --job-name=crc_test_ly --mem=8G  --array=0-6  scripts/generalized_modelling/gen_svm_test.sbatch data/matrix_internal_ly_test data/matrix_crc/svm_by_feature/svm_linear_pca150.0_gc_mapq30 prevelynch_test 30
sbatch --job-name=crc_test_ly --mem=16G --array=7-12 scripts/generalized_modelling/gen_svm_test.sbatch data/matrix_internal_ly_test data/matrix_crc/svm_by_feature/svm_linear_pca150.0_gc_mapq30 prevelynch_test 30

sbatch --job-name=crc_test_gs --mem=8G  --array=0-6  scripts/generalized_modelling/gen_svm_test.sbatch data/matrix_internal_gs data/matrix_crc/svm_by_feature/svm_linear_pca150.0_gc_mapq30 genoscan 30
sbatch --job-name=crc_test_gs --mem=16G --array=7-12 scripts/generalized_modelling/gen_svm_test.sbatch data/matrix_internal_gs data/matrix_crc/svm_by_feature/svm_linear_pca150.0_gc_mapq30 genoscan 30
```

fsr (13) skipped — CRC heavy training not yet run.

## Notebook additions (results.ipynb)

- **Cristiano ensemble CV**: data cell (dc4c5dd6), ROC with per-feature faded + ensemble bold black (54ff9b2b), AUC bar chart with ensemble in crimson (0b963bf0)
- **Cancer type ROC grid** (3×3): first panel = all cancers, then 7 types sorted by n descending, duodenal cancer excluded (69e65112, af61e945)
- **Stage ROC curves** for ensemble: 4 curves (I/II/III/IV) on one plot (138089c9, 88bd0f84)
- **CRC stage AUC** cells added previously (ad109548, c6d614f6) — file-guarded, running when CRC CV probs available
- tab20 recommended as color palette for 14-feature plots

## Memory backup

Claude memory files written and backed up to `protocols/claude_memory/`.

## Med test job memory issue — resubmitted at 24G

All med test jobs (7-12) were stuck at "Loading test matrix" for 5.5+ hours at 16G. Root cause: heavy training jobs (train_heavy + ly_train_heavy, each 64G) co-located on the same nodes consumed available RAM, causing test matrix reads to swap.

Cancelled and resubmitted at 24G:
```bash
scancel 1719764 1719762 1719892 1719895

sbatch --job-name=test_ly       --mem=24G --array=7-12  scripts/generalized_modelling/gen_svm_test.sbatch data/matrix_internal_ly_test data/matrix/svm_by_feature/svm_linear_pca150.0_gc_mapq30 prevelynch_test 30
sbatch --job-name=test_ly_train --mem=24G --array=11-12 scripts/generalized_modelling/gen_svm_test.sbatch data/matrix_internal_ly_test data/matrix_internal_ly_train/svm_by_feature/svm_linear_pca150.0_gc_mapq30 prevelynch_test 30
sbatch --job-name=crc_test_ly   --mem=24G --array=7-12  scripts/generalized_modelling/gen_svm_test.sbatch data/matrix_internal_ly_test data/matrix_crc/svm_by_feature/svm_linear_pca150.0_gc_mapq30 prevelynch_test 30
sbatch --job-name=crc_test_gs   --mem=24G --array=7-12  scripts/generalized_modelling/gen_svm_test.sbatch data/matrix_internal_gs data/matrix_crc/svm_by_feature/svm_linear_pca150.0_gc_mapq30 genoscan 30
```

Memory guideline update: **med test jobs need 24G** (not 16G) when heavy training jobs may be co-located.

## Ensemble nostd comparison

Ran ensemble 10×10 CV with standardize=False on the same meta-matrix:
```bash
sbatch --job-name=ensemble_cv_nostd scripts/generalized_modelling/gen_ensemble_svm.sbatch cv data/matrix/svm_by_feature/svm_linear_pca150.0_gc_mapq30/meta_matrix/meta_matrix.csv linear 10 none none none nostd false
```
Result: AUC = **0.9918** (vs 0.9932 with standardize=True — minimal difference as expected since features are already in [0,1]).
Notebook ensemble cells updated to use `ensemble_nostd/cv/cv_probs.csv` (cells dc4c5dd6, 97555f05).

## Pending

- cv_ly_med_train (1720090 at 32G): restarted from scratch after node outage
- cv_crc_med (1864): restarted from scratch after node outage
- CRC heavy (fsr): once crc_build_fsr done → cv_crc_heavy + train_crc_heavy → test fsr on LY/GS
- Cristiano train_heavy (1613) + ly_train_heavy (1614): running after node recovery
- test_ly, test_ly_train, crc_test_ly, crc_test_gs: resubmitted at 24G (above)

## Node outage

All gen-compute01-07 went DOWN simultaneously. All running jobs were killed and requeued. Nodes recovered and jobs restarted ~23min after recovery. cv_ly_med_train was the most costly (lost 3d20h of compute).

## Thesis visualisation strategy (7 days to submission)

**Cristiano-trained models (CV + external test) — show all available features:**
Full 14-feature plots where complete. Cristiano 10×10 CV is 100% done and directly comparable to Zhou et al. Cristiano→GS is also fully done (med completed pre-outage). Cristiano→LY: light complete, med will complete soon. This is the primary comparison section.

**CRC and LY trained models — EDM-feature focus as fallback:**
If med jobs do not finish before thesis submission, restrict external validation plots for CRC and LY to the 5 EDM-category features only (edm, iedm, eedm, eoedm, ocedm — all light, indices 1-5). These are 100% complete across all datasets and test conditions.

Rationale: the 4 novel features (iedm, eedm, eoedm, ocedm) are the primary scientific contribution. Showing them fully evaluated across all training configs and test datasets is sufficient for a complete results section. The med features (pfe, coverage, ends, ocf, ifs, wps, wps) can be noted as still running or omitted from cross-dataset comparisons without affecting the core narrative.

## Visualization goals (full list)

### Hyperparameter selection (all done)
1. MAPQ threshold comparison — 01_mapq.png ✓
2. Kernel RBF vs linear — 02_kernel.png ✓
3. PCA components — 03_pca.png ✓
4. GC correction — 05_gc_cv10.png ✓

### Cristiano CV per-feature (all done except score distributions)
5. Feature correlation heatmap — 07_feature_corr.png ✓
6. ROC curves all features — 08_roc_cv.png ✓
7. AUC bars vs Zhou — 09_auc_bars_cv.png ✓
8. Score distributions — violin/box cancer vs healthy per feature — MISSING
9. Stage-stratified AUC line plot — 06_stage_auc.png ✓

### Cristiano ensemble (all done except score distributions)
10. ROC per-feature vs ensemble — 20_roc_ensemble_cv.png ✓
11. AUC bars ensemble vs per-feature — 21_auc_bars_ensemble_cv.png ✓
12. ROC by cancer type 3x3 grid — 22_roc_cancer_type_grid.png ✓
13. ROC by stage I/II/III/IV — 23_roc_stage_ensemble.png ✓
14. Score distributions ensemble — cancer vs healthy — MISSING

### CRC CV
15. ROC curves — cell exists
16. AUC bars — cell exists
17. Stage-stratified AUC — 06b_stage_auc_crc.png ✓ (light only currently)

### External validation
18. Cristiano→LY: ROC + AUC bars
19. Cristiano→GS: ROC + AUC bars ✓ (complete pre-outage)
20. CRC→LY: ROC + AUC bars
21. CRC→GS: ROC + AUC bars
22. LY CV: ROC + AUC bars
23. LY→LY: ROC + AUC bars
24. LY→GS: ROC + AUC bars

### Summary figure (high priority, MISSING)
25. Cross-dataset AUC heatmap — feature × condition matrix (Cristiano CV, CRC CV, LY CV, Crist→LY, CRC→LY, LY→LY, Crist→GS, CRC→GS, LY→GS). Can be built with EDM features only as fallback.

## Notebook rebuild — results.ipynb (2026-05-11 afternoon)

Deleted all cells after the TODO cell (a00d29a4) and started rebuilding from scratch with:
- Consistent FEATURE_COLORS dict (tab20, fixed order) in setup cell f5fa7000
- NEW_FEATURES = {iedm, eedm, eoedm, ocedm} for old/new coloring
- Two new helper functions: plot_roc_auc_panel (2-panel ROC+AUC), plot_auc_bars_oldnew (blue=existing, red=novel)

**Already inserted (D + E sections):**
- D_crist (cell 11d2e756): Cristiano→LY + Cristiano→GS, 2-panel + old/new
- D_crc (cell cd2423b8): CRC CV + CRC stage + CRC→LY + CRC→GS, 2-panel + old/new
- D_ly (cell f6d5039e): LY CV + LY→LY + LY→GS, 2-panel + old/new
- E22 (cell 971082a3): Cross-dataset AUC heatmap (feature × 9 conditions)

**Still TODO (B + C sections — not yet inserted):**

### B — Cristiano 10×10 CV per-feature
- [ ] B5: Feature correlation heatmap (07_feature_corr.png)
- [ ] B6-7: ROC+AUC 2-panel (consistent colours) + old/new AUC bars variant
- [ ] B8: Score distributions — violin, cancer vs healthy per feature (NEW, never implemented)
- [ ] B9: Stage AUC line plot per feature (06_stage_auc.png, already worked, needs rewrite with FEATURE_COLORS)

### C — Cristiano ensemble
- [ ] C_data: Load ensemble_nostd CV probs, build ens_merge
- [ ] C10: ROC per-feature (faded, feature-coloured) + ensemble (bold black)
- [ ] C11: AUC bars ensemble vs per-feature (feature-coloured) + old/new variant
- [ ] C12: ROC by cancer type 3×3 grid (22_roc_cancer_type_grid.png)
- [ ] C13: ROC by stage I/II/III/IV + stage AUC line plot

**Decision needed:** confirm which of B/C cells to implement vs skip given thesis deadline.
