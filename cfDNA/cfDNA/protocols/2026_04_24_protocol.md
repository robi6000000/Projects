reason for long rutime of our svm model.
```probabilitybool, default=False
Whether to enable probability estimates. This must be enabled prior to calling fit, will slow down that method as it internally uses 5-fold cross-validation, and predict_proba may be inconsistent with predict. Read more in the User Guide.```

building matrices for gs dataset: 

```sbatch --array=0-13%4 scripts/generalized_processing/matrix_build_array.sbatch \
    30 ./data/internal_features_gs ./data/matrix_internal_gs/by_feature ./data/manifest/internal_metadata_gs.csv
```